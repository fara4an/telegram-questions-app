// В src/bot.js обновляем обработчик /start:

bot.start(async (ctx) => {
    const userId = ctx.from.id;
    const firstName = ctx.from.first_name || 'друг';
    
    // Если есть start payload (кто-то перешел по ссылке)
    if (ctx.startPayload && ctx.startPayload.startsWith('ask_')) {
        const targetUserId = ctx.startPayload.replace('ask_', '');
        
        // 1. Отправляем ПРИВЕТСТВИЕ с большой кнопкой
        await ctx.reply(
            `👋 *${firstName}, привет!*\n\n` +
            `Ты перешёл по ссылке, чтобы задать *анонимный вопрос*.\n\n` +
            `Нажми на кнопку ниже 👇 чтобы *сразу написать вопрос*:`,
            {
                parse_mode: 'Markdown',
                reply_markup: {
                    inline_keyboard: [
                        [
                            {
                                text: '✍️ НАПИСАТЬ АНОНИМНЫЙ ВОПРОС',
                                web_app: { 
                                    url: `${process.env.WEB_APP_URL}/ask/${targetUserId}?from=telegram` 
                                }
                            }
                        ],
                        [
                            {
                                text: '❓ Как это работает?',
                                callback_data: 'how_it_works'
                            },
                            {
                                text: '📱 Открыть своё приложение',
                                web_app: { url: process.env.WEB_APP_URL }
                            }
                        ]
                    ]
                }
            }
        );
        
        // 2. Через 2 секунды отправляем инструкцию
        setTimeout(async () => {
            try {
                await ctx.reply(
                    `*📌 Всё просто:*\n\n` +
                    `1. Нажми кнопку "НАПИСАТЬ АНОНИМНЫЙ ВОПРОС"\n` +
                    `2. В открывшемся окне напиши вопрос\n` +
                    `3. Нажми "Отправить анонимно"\n` +
                    `4. Готово! Вопрос придёт получателю\n\n` +
                    `*🔒 Анонимность гарантирована!*`,
                    { parse_mode: 'Markdown' }
                );
            } catch (error) {
                // Игнорируем ошибки
            }
        }, 2000);
        
    } else {
        // Обычный старт - показываем профиль пользователя
        const userLink = `https://t.me/${ctx.botInfo.username}?start=ask_${userId}`;
        
        await ctx.reply(
            `👋 *Привет, ${firstName}!*\n\n` +
            `*Твоя персональная ссылка для вопросов:*\n\`${userLink}\`\n\n` +
            `*Отправь эту ссылку друзьям* 👇\n` +
            `Они смогут задать тебе вопрос *анонимно*!`,
            {
                parse_mode: 'Markdown',
                reply_markup: {
                    inline_keyboard: [
                        [
                            {
                                text: '📱 ОТКРЫТЬ МОЁ ПРИЛОЖЕНИЕ',
                                web_app: { url: process.env.WEB_APP_URL }
                            }
                        ],
                        [
                            {
                                text: '📤 ПОДЕЛИТЬСЯ ССЫЛКОЙ',
                                url: `https://t.me/share/url?url=${encodeURIComponent(userLink)}&text=Задай%20мне%20анонимный%20вопрос!`
                            }
                        ]
                    ]
                }
            }
        );
    }
});

// Обработка кнопки "Как это работает?"
bot.action('how_it_works', async (ctx) => {
    await ctx.answerCbQuery();
    await ctx.reply(
        `*📌 Как работает анонимный вопрос:*\n\n` +
        `1. Ты получаешь ссылку друга\n` +
        `2. Переходишь по ссылке (ты здесь)\n` +
        `3. Нажимаешь "НАПИСАТЬ АНОНИМНЫЙ ВОПРОС"\n` +
        `4. Пишешь вопрос в открывшейся форме\n` +
        `5. Нажимаешь "Отправить анонимно"\n\n` +
        `*Что происходит дальше:*\n` +
        `✅ Вопрос *анонимно* приходит получателю\n` +
        `✅ Он получает уведомление в Telegram\n` +
        `✅ Он может ответить на вопрос в приложении\n` +
        `✅ Ответ придёт тебе (если ты не аноним)\n\n` +
        `*🔒 Анонимность:*\n` +
        `• Получатель *не увидит* твой профиль\n` +
        `• Можно задавать сколько угодно вопросов\n` +
        `• Вопросы хранятся только у получателя`,
        { parse_mode: 'Markdown' }
    );
});